from temp import Temp

if __name__ == '__main__':
    print("Balconia Roast Logger")
    temp = Temp()
    #temp.start_measuring()
    # create_csv(filename, columns)
    # add_row(filename, columns, "eas", "adb", "sc")
